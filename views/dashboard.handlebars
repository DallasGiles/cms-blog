<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
</head>
<body>
    <h1>Welcome, {{username}}!</h1>
    <h2>Your Posts</h2>

    {{#if userPosts.length}}
        <ul>
            {{#each userPosts}}
                <li>
                    <h3>{{this.title}}</h3>
                    <p>{{this.content}}</p>
                    <small>Posted on {{this.createdAt}}</small>
                    <!-- Delete Button -->
                    <button class="delete-post-btn" data-id="{{this.id}}">Delete Post</button>
                </li>
            {{/each}}
        </ul>
    {{else}}
        <p>You have not created any posts yet.</p>
    {{/if}}

    <a href="/create-post">Create a New Post</a>
    <form action="/logout" method="POST">
        <button type="submit">Logout</button>
    </form>

    <!-- Include JavaScript for handling the delete functionality -->
    <script src="/js/dashboard.js"></script>
</body>
</html>